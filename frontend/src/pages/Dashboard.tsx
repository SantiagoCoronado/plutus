export default function Dashboard() {
  return (
    <div className="rounded-lg border border-dashed border-zinc-800 p-10 text-center">
      <h2 className="mb-2 text-xl font-medium text-zinc-300">Dashboard</h2>
      <p className="text-sm text-zinc-500">
        Phase 1 ships the data foundation (ingestion, storage, API). The dashboard — market
        strip, portfolio cards, heatmap treemap, opportunity inbox — arrives with Phases 2–5.
      </p>
    </div>
  )
}
